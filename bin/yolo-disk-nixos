#!/usr/bin/env bash

set -xe

test "$(id -u)" -eq 0 || exit 1
grep -- nixos /etc/os-release || exit 1
grep -- vagrant /etc/passwd || exit 1

fdisk /dev/vda <<EOF
d
2
n
2


w
EOF

resize2fs /dev/vda2
reboot
