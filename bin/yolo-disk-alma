#!/usr/bin/env bash

set -xe

test "$(id -u)" -eq 0 || exit 1
grep -- almalinux /etc/os-release || exit 1
grep -- vagrant /etc/passwd || exit 1

fdisk /dev/vda <<EOF
d
4
n
4


w
EOF

xfs_growfs /dev/vda4
reboot
