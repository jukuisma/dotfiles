#!/usr/bin/env bash

set -xe

if [ "$(id -u)" -ne 0 ]; then
    exit 1
fi

if ! grep -- almalinux /etc/os-release; then
    exit 1
fi

if ! grep -- vagrant /etc/passwd; then
    exit 1
fi

fdisk /dev/vda <<EOF
d
4
n
4


w
EOF

xfs_growfs /dev/vda4
reboot
