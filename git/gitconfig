[user]
    email = <EMAIL>
    name = Juho Kuisma
    signingkey = <KEY>

[push]
    default = simple

[pull]
    ff = only
    rebase = false

[color]
    diff = auto
    status = auto
    branch = auto
    interactive = auto
    ui = true
    pager = true

# [commit]
#     gpgsign = true

# [tag]
#     gpgsign = true

[rebase]
    autosquash = true

[gpg "ssh"]
    allowedSignersFile = ~/.ssh/allowed_signers

[pager]
    branch = false
#     diff = "diff-so-fancy | less"
#     show = "diff-so-fancy | less"
